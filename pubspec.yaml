name: saad_trading_bot
description: "Core Mechanical Execution Terminal for Bybit Demo Trading Environment."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.0
  crypto: ^3.0.3
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true